{
  "mcpServers": {
    "stripe": {
      "url": "https://mcp.stripe.com"
    },
    "vercel": {
      "url": "https://mcp.vercel.com"
    },
    "resend": {
      "command": "npx",
      "args": ["-y", "resend-mcp"],
      "envFile": "${workspaceFolder}/.env.local"
    }
  }
}
