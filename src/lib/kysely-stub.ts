// Empty stub — this module is never used at runtime (we use the Drizzle adapter).
// It exists to satisfy Turbopack's static analysis when it traces
// @better-auth/kysely-adapter SQLite dialects that reference removed kysely exports.
export default {};
